[project]
name = "mlwd-collector"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["numpy", "vllm", "transformers", "matplotlib"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"

[tool.setuptools.packages.find]
include = ["mlwd*"]
